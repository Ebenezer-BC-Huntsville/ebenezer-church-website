# Copy this file to .env for local development (and add real values to
# Vercel's dashboard for production). Never commit a file called .env —
# it's already excluded in .gitignore.

GMAIL_ADDRESS=youraddress@gmail.com
GMAIL_APP_PASSWORD=your16characterapppassword
CHURCH_CONTACT_EMAIL=office@visiteb.church
